PROGRAM step4
BEGIN

	INT a, b, i, p;
	STRING newline := "\n";

	FUNCTION VOID main()
	BEGIN

		i := 0;
		a := 0;
		b := 0;

		DO
			READ (p);
			
			IF (p > 10) 
				a := a + 1;
			ELSE
      			b := b + 1;
			ENDIF

			i := i+1;

		WHILE(i != 10)

		WRITE (a, newline);
		WRITE (b, newline);
	END
END
